# SGM Environment Configuration

# Binding Configuration
# Modes: synthetic (in-memory), mapped (external API), live (Prisma DB)
BINDING_MODE=synthetic
BINDING_MODE_DOCUMENT=synthetic
BINDING_MODE_COMMITTEE=synthetic

# External API Configuration (for mapped mode)
EXTERNAL_API_URL=https://api.example.com
EXTERNAL_API_KEY=your-api-key-here
API_TIMEOUT=5000

# Database Configuration (for live mode)
# CRITICAL: Must include &schema=sgm_summit_demo for proper schema isolation
# Format: postgresql://user:password@host:port/database?sslmode=require&schema=sgm_summit_demo
DATABASE_URL=postgresql://user:password@db.prisma.io:5432/postgres?sslmode=require&schema=sgm_summit_demo
DATABASE_URL_DIRECT=postgresql://user:password@db.prisma.io:5432/postgres?sslmode=require&schema=sgm_summit_demo
DB_POOL_SIZE=10

# File Storage Configuration
# For S3 storage in production:
# AWS_REGION=us-east-1
# AWS_BUCKET_NAME=sgm-documents
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_SITE_URL=http://localhost:3003

# Tenant Configuration
DEFAULT_TENANT_ID=demo-tenant-001

# Authentication (NextAuth.js)
NEXTAUTH_URL=http://localhost:3003
NEXTAUTH_SECRET=your-nextauth-secret-here-generate-with-openssl-rand-base64-32

# OAuth Providers
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Optional: GitHub OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Optional: Apple OAuth
APPLE_CLIENT_ID=your-apple-client-id
APPLE_CLIENT_SECRET=your-apple-client-secret
